# Example environment configuration for TON‑RODY

# Node environment
NODE_ENV=development

# Application ports
PORT=3001

# Database connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tonrody

# Telegram bot token used to verify initData signatures
TELEGRAM_BOT_TOKEN=

# Secret used for signing JSON Web Tokens (JWT)
JWT_SECRET=

# TON provider API keys and URLs
TON_API_KEY=
TON_PROVIDER_URL=https://tonapi.io
TON_PROVIDER_FALLBACK_URL=https://toncenter.com

# TON Connect manifest URL – must be publicly accessible
NEXT_PUBLIC_TON_CONNECT_MANIFEST_URL=http://localhost:3000/tonconnect-manifest.json

# Fee recipient address for games (owner’s wallet)
FEE_RECIPIENT_ADDRESS=EQ...................................

# Other optional configuration goes here